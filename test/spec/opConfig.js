describe('Service: opConfig', function () {
    'use strict';

    // load the service's module
    beforeEach(module('opApp'));

    it('should succeed', function () {
        expect(true);
    });
});
